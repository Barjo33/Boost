---
name: v7-design-2026-07-07
description: "Overnight 'rebuild the algorithm' design (Tim's brief: higher TIR, no announcements, minimal setup, anything goes) — drafted, RED-TEAMED, revised. VERDICT: don't rebuild the organism (base engine + paired comparison IS the measurement instrument); TWO transplants survive: (1) distributional dose-sizing under the EXISTING states/guards/v1-bounds (kills brake-stack class structurally), (2) efficacy DAMPER-ONLY + suspicion-flag channel (two-sided θ is DEAD: Episode-B fix and sensor-drift catastrophe are the same scalar with opposite correct actions). Realistic gain +2-4pp TIR in duration-over-160. Three zero-risk backtests specced as first actions."
metadata:
  type: project
---

**V7 design exercise (2026-07-07 overnight).** Files delivered to Tim: V7_design_FINAL_2026-07-07.md + draft (session scratchpad — re-send from repo if lost; consider committing to backtesting/reports/ or docs/ if Tim endorses).

**What the red-team KILLED (keep these as permanent constraints):**
1. Two-sided efficacy state θ — sensor-drift/compression/occlusion all read as "insulin weak" and demand LESS insulin while θ demands MORE; positive feedback with the guard blinded (P(low) computed from the same drifted CGM). Occlusion+micro-dosing defeats pump pressure detection. No τ works (fast=noise-chaser, slow=misses the 90-min episode). Unidentifiable at low IOB.
2. Self-estimated hypo tail as PERMISSION — off-policy circularity (tail thin BECAUSE the conservative policy suppressed it; bias direction fixed); chance constraints may only TIGHTEN, never grant what existing guards refuse.
3. Deleting the state machine — session lock encodes real double-dose incidents; hysteresis is state, not a distribution feature; stateless argmax + anything holding P(low) down rebuilds multi-CONFIRMED.
4. Zero-setup as foundation — naive users need population defaults; zero-knob is the per-user ASYMPTOTE via continuous re-derivation.
5. "Doses earlier" — interstitial lag gates everything to the same rise; honest claim = faster RAMP + never self-strangles = duration-shaving. Early honest distributions are WIDE → naive utility doses SMALLER early; the meal states survive as the distribution-narrowing prior.
6. Dropping the non-meal v1-bound = losing the paired-comparison methodology itself (M3 — the strongest single point).

**What SURVIVES (the roadmap if Tim proceeds):**
- Transplant 1: distributional sizing — argmax E[U] over oref-short-horizon + measured residual quantiles (center/shoulders only), inside the existing V6 skeleton (states as prior, all guards/caps/v1-bounds unchanged, strictly-re-gated carry-forward register). Kills brake compounding + sub-pump-step starvation structurally. Fully validatable with existing apparatus.
- Transplant 2: efficacy DAMPER (sustained better-than-predicted action trims dosing + widens guard — generalizes the F2 exercise fix; catches festival-crash class regardless of cause) + SUSPICION FLAG (sustained under-performance at credible IOB → notification with site-age/sensor-plausibility, NEVER a dose; Episode-B's judgment half).
- Expected: +2-4pp TIR, concentrated in time-over-160 DURATION, TBR flat by construction.

**FIRST ACTIONS (zero-risk backtests, data in hand, per protocol):** (1) per-user residual quantile fits + PIT calibration + the tail-honesty check we EXPECT to fail (documents why ε never grants); (2) efficacy-innovation reconstruction across 9 users — damper-side discrimination of exercise/fresh-site + whether ANY observable combo separates Episode-B from sensor-drift (teeth for the flag); (3) full distributional-sizing replay of the month's cycles priced by [[two-test-bar-2026-07-06]].

See [[phase3-brake-compounding-2026-07-06]] (the brake-stack evidence), [[hr-glucose-leadlag-null-2026-07-06]], [[resume-point-2026-07-07]].

**FOUNDATION BACKTESTS RUN (2026-07-07 morning, protocol-compliant — scripts+report in backtesting/, committed after the batch):**
1. **Substrate GO**: IOB-projection residual quantiles well-calibrated across 8 users × 3 horizons (odd/even validation). BUT median bias +12..+38 mg/dL (unannounced carbs live in residuals; C inverted −8) and the tail-honesty check failed as predicted (15–99 <70-events per user inform the 5% bucket; 1% tail unestimable) → chance constraints tighten-only, PERMANENTLY documented.
2. **Efficacy signal: NO TEETH both sides.** Damper: festival-vs-quiet Cohen's d=0.02 — variable_sens has ALREADY ABSORBED the efficacy signal (the projection uses adapted sens). One concrete follow-up: re-test with SENS-FROZEN projections. Flag: innovation ranks Episode A ABOVE Episode B (+24.2 vs +17.4) — cannot separate; only SITE AGE separates (1.7h vs 69.4h, n(true)=1). Red-team predictions empirically confirmed.
3. **Distributional sizing: NO-GO as formulated.** Cost-ratio insensitive (R=4/7/10 identical — the safety knob does nothing because the biased q25 never projects <70); rides the median bias (+205U at 120-160, +75U BELOW 120; Test A FAIL B/C/D/F, pass tim/A/E/H). Restraint preservation works (H budget=0 → 0). Revised acceptance criteria in the report: R-sensitivity must appear; quiet-flat cycles must project ≈0 drift (debias/condition residuals first).
4. **DATA CORRECTION affecting the ACTIVE floor**: Episode-B's zeroed 13:43-14:08 stretch — RECORDED v1_units was 0 (the forensic's 0.3/cycle was reconstruction) → the RECOVERING non-meal v1-bound zeroes the floor's uplift THERE too. The floor's Episode-B benefit flows mainly through COMMITTED cycles (not v1-bounded); Tim's live floorWouldAdd/applied telemetry adjudicates the real value before the 07-10 cohort activation review. Not a safety issue (floor is bounded; worst case adds nothing).

**V7 status: substrate solid → sizing back to formulation; damper awaits sens-frozen retest; flag shelved with evidence. The discipline worked: NO-GO cost zero — nothing shipped.**

**V7 SHADOW BRANCH LIVE (07-07 afternoon): `Boost-V7-shadow` @ 16b2d98620 (pushed), off experimental 1a81fee60a.** Package openAPSBoostV7/ (V7ResidualTracker + V7InnovationTracker + V7Sizer + V7Shadow orchestrator, lambda seams no DI change, double-runCatching, invoked after V5 runShadow / before the V6 override so rT.units is still V1's dose). KEY IMPLEMENTATION FINDING: the offline R-insensitivity was partly STRUCTURAL — the 3-point {q25,q50,q75} distribution makes one sub-70 quantile outweigh everything at any R≥4; on-device sizer uses a 19-point piecewise-linear inverse CDF from the 5 pool quantiles → synthetic debiased pool yields R4=1.65>R7=1.60>R10=1.40 (pinned test) while biased user-A quantiles reproduce identical-3.0 pathology. Pools regime-conditioned (MEAL/NIGHT/QUIET_FLAT; rising non-meal daytime EXCLUDED), ~21d window, warm@150, persisted StringKey.ApsBoostV7ResidualPools. Telemetry: boostV7_{wouldDoseR4,R7,R10,pLow90,q50Drift,pool,innovSensFrozen} + breadcrumb only when R-doses differ; extractor wired (7 cols). 39 V7 tests + 111 V5 regression green incl. dosing-path BIT-IDENTITY. **APK `Boost-V7-shadow-2026-07-07.apk` on Drive — supersedes sensinghardening for Tim's phone (branch cut from 1a81fee60a so it contains the whole sensing batch + floor toggle); wear APK unchanged (Boost-WEAR-hrwatchdog-2026-07-07). Field read-out: R-doses diverging = criterion (a) alive; quiet_flat q50Drift≈0 = criterion (b) fixed; then reformulation review.** Branch maintenance: rebase/merge from experimental as it moves.
