---
name: user-h-diagnosis-2026-07-05
description: "user H = a DB cohort user ([user-H-site], token in sites.json). V6-ACTIVE since 06-30, complaint 'not enough insulin early': CONFIRMED = config hygiene — TDD ~50U on factory committedCap 0.5 vs HIS formula 1.24 (44% of meal gap cap-clipped, vs cohort 28%). Recommended 0.5→1.2 + confirmedCap 4→6. TWO CODE LEADS: auto-config never applied committedCap on his device; RECOVERING-during-rise = 28% of his gap."
metadata: 
  node_type: memory
  type: project
  originSessionId: db82de70-d40e-4e73-9c47-395352be1ee8
---

**user H — user H (added to sites.json 2026-07-05).** Samsung S948B, mmol, Bratislava UTC+2, TDD ~49.6U, announces meals (~3 boluses/day median 3.5U). V1/V2-acting from 05-05, **V6-ACTIVE since 06-30** (complaint week = pre-safety-layer build; updated to ~8ecaf7bbd9-era build on 07-05). 22k decisions pulled to DB.

**Complaint validated**: V6 era vs V1 era — TING 79→73%, >180 2.8→5.4%, meal peaks 161→179, min>140/episode 68→106. First-90-min of 19 meal episodes: V6 delivered 24.8U vs base-would 40.9U. BUT hypos halved (<70 2.21→0.81%) — his V1 era was ABOVE hypo target, so some V6 restraint was correction, not deficit.

**Gap decomposition (his 21.7U meal gap)**: cap-clipped COMMITTED **44%** (14 cycles pinned at 0.50 while base wanted 1.20/cycle; 60% of his COMMITTED dosing cycles at-cap) — he's the exception the cohort rule allows: **factory committedCap 0.5 vs his auto-config formula 1.24 (TDD/40)**. RECOVERING-during-rise **28%** (0.4× while still climbing — no user knob; code-side). OBSERVING 14%. Profile NOT the problem (95% TIR in V1 era). Confirm-floor coupling checked: raise to 1.24 adds only ~3/20 delayed confirms — net positive FOR HIM (unlike cohort average).

**RECOMMENDED (sent to Tim 07-05)**: committedCap 0.5→**1.2** (or 1.0 cautious), confirmedCap 4.0→**6.0** (his p90-bolus formula; floor stays committedCap-governed), stay on the 07-05 build, re-measure ~7 days, watch TBR<70 (raise moves him back toward his old hypo pattern; hypoCaution 1.0 ok for now).

**CODE LEADS for us**: (1) **auto-config miss ROOT-CAUSED + FIXED 2026-07-06 (`b2c0705e5e`, local)**: two real defects — (i) "user-touched" test was raw key-PRESENCE (`getIfExists != null` / `sp.contains`) not value-vs-default, so settings import or opening a pref dialog at stock value blocked suggestions forever; (ii) GLOBAL one-shot flag (travels via settings export!) meant later-added derivations never reach existing installs. Fixed: per-knob resolution (BooleanComposedKey.BoostV5AutoConfigResolved), value-vs-default test (>1e-4), migration = legacy-flag users get knobs-at-stock re-derived (the user H rescue; off-default left alone), flag cleared. NOTE: cumulative-cap-never-applied suspicion was FALSE (derived+applied+notified since 3717d5f0ef 06-26; my earlier line citation was stale); Tim's 2.5 = genuinely user-set, correctly skipped. Field caveat: GeneralSimpleMode=true masks stored V6 knobs (defaultedBySM) — user H verified FULL mode (his 1.2/6 caps show in live RT). 19 tests green. user H gets rescued when he installs a build with the migration. (2) RECOVERING-during-rise leak (28% of his gap) — distinct population from the REJECTED post-confirm-high levers ([[recovering-highs-smb-rejected-2026-07-03]]): these are ANNOUNCED-meal mid-rise decel→RECOVERING episodes where steady climbs miss the deltaAccl>10 re-engage. Adds per-user weight but doesn't overturn the cohort verdict; revisit only with its own backtest.

Caveats: only 5.5 days V6-ACTIVE (~19 episodes); base-would = no-feedback counterfactual; pre-telemetry caps inferred from dose ceilings (clean 0.5/2.5 fingerprints; 4 cycles at 0.75 on 07-02 = his knob experiment).

**BUDGET-SIDE INVESTIGATION CLOSED (2026-07-06 eve): V6 is RIGHT by outcome on his data — design decision, not a bug.** Verified: budget == max(0, oref insulinReq) EXACTLY (96% of cycles to the cent; NO velocity term; mlHypoRisk irrelevant). His budget=0 climbs = unannounced eating (COB=0) with his own bolus IOB 3.5-4U aboard + dynISF strengthening as BG climbs → predictions said "covered" — and were right **5/5**: every V6-era budget=0 >180 stretch resolved <160 within 90min of stretch-end on existing IOB, 0 lows, eventualBG unbiased at +60min (conservative at +120). V1 was faster only because tiers added ~0.5U/cycle of velocity insulin his predictions called unnecessary (harm-neutral for him: 10.5% vs 10.6% base; bought SPEED of descent, not missing correction). ISF recalibration DEAD (predictions unbiased). **Actions: (1) tell user H his >180 excursions run ~0.8-1U under-covered each and the stuck ones were UNANNOUNCED night/morning eating — behaviour closes more than code; (2) optional per-user opt-in 'velocity-budget floor' (budget := max(insulinReq, tier-equivalent) when delta>3 ∧ BG>180, committedCap-bounded, shadow-first) — defensible FOR HIM (+1.6 U/day, historically harm-neutral), DO NOT ship cohort-default (thrice-rejected class for rebound-driven users); (3) complaint is time-at-high, not missing insulin — V6 trades V1's speed for prediction-led restraint, and his data endorses the trade.**

**user A (user A) — same-complaint analysis (2026-07-06 eve, latest-model replay):** V6-ACTIVE, pure UAM (0 announcements). TING 61.5%, >180 3.3h/day. DOMINANT mechanism = committedCap clipping (~10 holds/day pinned at factory 0.5, budgets >1U) → the amended migration derives 1.09 (TDD-window-sensitive 1.09–1.21) and restores +4–6 U/day at 2.3% pre-low vs 7.6% base — his complaint ≈ solved by INSTALLING THE UPDATE. Also: early-confirm moves 10/79 confirms earlier + 2 fast-path catches (free); amended confirmedCap 2.5→2.33 (n=4 boluses → SMB path — the min-sample guard working); gate floor pinned at 0.5, no regression; shadow floor prices mediocre for him (12.3%, leave shadow); ~¼ of his high time (0.85h/day) = budget=0/insulinReq≤0 Episode-A class, deliberately untouched — re-adjudicate at +7d, velocity-budget conversation only if the residual persists. Kill-switch: cap back to 0.5 if TBR<70 doubles from 1.27%. Ready-to-send user statement in the 07-06 report.
