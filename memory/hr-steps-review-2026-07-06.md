---
name: hr-steps-review-2026-07-06
description: "HR/steps subsystem review (07-06 night): F2 = NEW SAFETY DEFECT — live V1 engine never fills v5_exerciseActive/inPostExerciseWindow (only retired V3MLG3 did) so ALL V6 exercise protections are dead (score damp, fastConfirm gate, post-ex budget damper) — 3-line fix from V3MLG3:1181. Plus F1 no-steps guard spec, F3 dosing-steps are phone-only, F4 no HR watchdog, F5 smoothing sites (CHECK WATCH 'HR smoothing' PREF — may be the DB blunting), F6 dark-source never alerts. Tonight = wake-fix first live test; 7-item checklist."
metadata:
  type: project
---

**HR/steps review (2026-07-06, read-only, @ e1b8803560).** Full report in the agent output; ranked findings:

- **F2 SAFETY DEFECT (new)**: OapsProfileBoost built at OpenAPSBoostPlugin:1119-1206 omits v5_exerciseActive/v5_inPostExerciseWindow/v5_exerciseSubclass (defaults false; only OpenAPSBoostV3MLG3Plugin.kt:1181-1186 fills them; V5 reads at OpenAPSBoostV5Plugin:505-507). V6 always believes "not exercising": MealSignalScore notExercising term dead, fastConfirm fires during exercise, AggressionBudget post-exercise damper never engages. Connects to the festival exercise-into-correction hypo class. FIX: copy the 3-line block (activityResult.activityState + recoveryWindowEnd in scope).
- **F1**: no-steps→INACTIVE(130%)+2h sleep-in defect, located: StepService.kt:57-84 returns 0 from empty buckets, previousStepCount==-1 marker unexposed. SPEC: @Volatile lastEventMs + feedState(NONE|LIVE[|STALE 6h]); stepsAvailable = phone LIVE || SC row ≤12min; if unavailable skip INACTIVE + sleep-in, breadcrumb steps:UNAVAILABLE + RT field.
- **F3**: dosing activity classification uses StepService ONLY (plugin:560-563); wear steps never reach isActive/HR-fusion → watch-worn+phone-idle = false INACTIVE + RESISTANCE/STRESS misreads (HR zones + phone-steps<30). Fix: max(phone, fresh WearStepSource windows) — SC rows carry all four windows.
- **F4**: Oppo HR listener — registerListener bool ignored (HeartRateListener.kt:74), created once per service life, no watchdog. Fix: wear-side re-register on N-min silence + phone-side fresh→dark transition notification.
- **F5**: smoothing sites: (1) WATCH PREF heart_rate_smoothing (default 1=off, up to 15 — CHECK TIM'S TONIGHT; if 15, DB blunting is settings-only); (2) telemetry already has hrBpmLatest/Avg5m/Avg15m — extractor may map the wrong one; (3) ApsBoostHrWindowMinutes 15 for classification (intentional). Cheap transient exposure: hrBpmMax5m/Min5m RT fields (~2 lines at plugin:1516).
- **F6**: source-went-dark is never an event (07-04 battery night invisible). Fix: edge-detect anyFresh per feed → reason-line + notification after 60min dark daytime.
- F7: HC seedToday uses chosen-source total → can contaminate the phone-units calibration frame (use phone-device HC total instead). F8: hold-higher coherent; caveats = truncated-day undercount enters history; raw→calibrated wear flip causes one silent baseline step-change. F9: resolver phone-fresh = today>0 (HC seed makes dead sensor look live). F10: 5-min bucket lag; wear slot-max undercounts bursts; ACTIVE-150 meal-climb override still absent (Design 9).

**TONIGHT'S CHECKLIST (wake-fix first live test)**: (1) morning AWAKE at real wake ≠ 07:00, wakeReason=hr_steps; (2) sleepEntryReason=hr not drought; (3) hrSource_states fresh all night; (4) check watch HR-smoothing pref (→1 if 15); (5) hrLearnedDaytimeBpm populated? (was due 07-04; if null check sleepLearnedSessionCount vs ≥30-daytime-samples gate); (6) midnight stepHistory breadcrumb + (raw) flag; (7) morning sleep-in releases on >250 steps not the 2h backstop.

**PROPOSED NEXT BATCH (one build)**: F2 exercise-inputs fix (priority — live safety gap) + F1 availability guard + <54 co-guard ([[two-test-bar-2026-07-06]]) + F4 watchdog + F5 hrBpmMax5m + F6 dark-transition breadcrumb. Then Trio port of the batch + floor/pin/telemetry commits.

**BATCH SHIPPED (2026-07-07 morning, experimental @ 1a81fee60a, dev still held):** F2 exercise wiring `2a9d096d8b` (V6 exercise protections live for the first time; fastConfirm-blocks-when-exercising proven at decide() level), F1/F9 availability guard `fb3312a1e1` (+ 30-min resolve-grace fixing the wear/phone flapping), <54 co-guard `13c9bc4d53`, F4/F5/F6 HR hardening `729788d733` (wear watchdog re-registers after 5 silent min; HrFeedDarkTracker edge notes + 60-min daytime notification; hrBpmMax5m/Min5m; minGuardBG reason now annotated with source curve e.g. "(COB+UAM)"), rollover fix-fix `606c311d56` (IntradayStepBank — per-source running maxima banked every cycle, day-close from the bank; root cause was wear resetting at device-midnight before close read it). +V7 artifacts committed `1a81fee60a`. Tests 193+111+10 green. **APKs on Drive: phone `sensinghardening-2026-07-07` + wear `Boost-WEAR-hrwatchdog-2026-07-07` (manually apksigner-signed — :wear has no release signing config; same keystore as :app).** OVERNIGHT 07-06/07 findings driving this: HR dark all night (~2 readings/hr, charged watch) → PRE_SLEEP starvation 7.5h (third failure mode; timeout wake 08:03); rollover undercount recurred (wear 4142 lost → 739); sleep-in PASSED (released on real steps; live threshold 75 not 250); **overnight hypo: 22:44 3.0U CONFIRMED shot (prospective 5.19, base-would 2.0) on unannounced snack → nadir 52, 2h10 <75 — confirm-overshoot class, now with full telemetry; the distributional-sizing backtest replays it; watch his 14d TBR<54**. Tim force-stopped wear 10:20ish → HR streaming at full 1/min again (verified 09:23Z hr15m=14). Tonight = first VALID night for wake fix + SLEEPING entry + baseline warmup.
