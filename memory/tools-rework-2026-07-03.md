---
name: tools-rework-2026-07-03
description: "2026-07-03: simulator + tuning guide reviewed vs code/README then reworked into V1|V6 tabs with all fixes; 4 commits LOCAL on Boost-V6-experimental (push to dev/Pages HELD pending Tim's preview). Key finds: simulator missed the whole July-2 safety batch; guide had INVERTED activity-% semantics; README had 3 defects (cumulative cap 0-10(10), Aggression=CONFIRMED-only, two-trained-models wording)."
metadata: 
  node_type: memory
  type: project
  originSessionId: db82de70-d40e-4e73-9c47-395352be1ee8
---

**Tools review + rework session (2026-07-03), in ~/StudioProjects/Boost-AAPS-core on Boost-V6-experimental.**

**Review found:** simulator (last touched c166ee524d) was missing ALL 2026-07-02 safety fixes and showed removed behaviours (V6 out-dosing V1 in IDLE; caps 1.0/0.25; no confirm-adequacy gate; no rescue guard; age off-by-one confirming a cycle early). Tuning guide's July-2 rewrite was clean BUT Activity/Inactivity profile-% semantics were INVERTED in 5 places (code: ISF ÷ profileScale, basal × profileScale → <100% = LESS insulin) + flipped spectrum gradients (HypoCaution, BG Cap, Sleep-in ×2). README itself had 3 defects: cumulative cap said 0–5(1.5) but code = 0–10(10) non-binding; Aggression called "global" but scales CONFIRMED only; §7 "only trained model is hypo-risk" contradicted §1's ML meal-likelihood (weight 0.20 in confirm score).

**Shipped (4 LOCAL commits, tree clean, NOT pushed):** `62ce042a4c` leftover V5-doc updates (found uncommitted in tree, likely from 07-02 late session); `c5a0ec1a88` simulator V1|V6 tabs + full July-2026 engine port (41/41 node assertions vs Kotlin, pure ENGINE block now node-testable); `554e3651ea` guide V1|V6 tabs + all fixes + disclaimer; `4fbce7d31c` README corrections + July-guards documented in §3. Both tools sent to Tim for preview.

**AFTERNOON — early-dosing implementation (same day):** after the audit ([[early-dosing-audit-2026-07-03]]), 3 more LOCAL commits: `242a6e179d` sustained-score early confirm (age-gate −1 when score≥0.55 held 2 cycles; scoreReadyStreak caller-input pattern, lastCycleScore in V5PersistedState in-memory only), `d2f9a08108` fast-path retune Δ≥6/accl≥10/score≥0.65 (simulator+guide updated in same commit, sim now interpolates constants), `6067ec9a6d` gate telemetry boostV5_{confirmGate,prospectiveShot,aggressionKnob} (RT 10→13 fields; extractor edit in ~/oref-investigations-boost-v2 UNCOMMITTED — not a git repo). Tests 60/60, compile green. **APK `Boost-V6-experimental-earlyconfirm+fastpath+gatetelemetry-2026-07-03.apk` on Drive — Tim running it for a couple of days** to judge personally + decide whether early-confirm/fast-path should become per-user (auto-config-style derivation, NOT online learning — no-training tenet).

**EVENING — Analyser Trio mode + DB refresh:** analyser commit `e084e1fc64` (8th unpushed): per-dataset auto-detect (structured boostV5_* → AAPS; `boostV5[` reason tag or device "Trio" → Trio mode "Trio oref (acting) vs Boost V6 (shadow)"), mixed-site segmentation, AAPS proven byte-identical, 17/17 node tests. Trio tag format verified in source (BoostV5Adapter.reasonTag:344-362; tag DOES reach NS via det.reason += , survives the mmol rewriter). Extractor (~/oref-investigations-boost-v2, NOT git) now parses the tag → trio-shadow/active/off variants, v1_units=trio-oref units; 0 classification changes on 108k-row AAPS regression.

**PREMISE CORRECTION then RESOLUTION — Trio cohort.** The original 7 sites held ZERO Trio data (verified 3 ways; Tim initially believed otherwise). The `boost-other` variant rows = the AAPS **V6-experimental Kotlin consoleError format** — users A (06-30), B (06-30), E (06-29), F (06-29) + tim (06-26) upgraded to V6 builds; C/D still v1-format. Deliberately NOT re-mapped. DB refreshed all 7 through 2026-07-03 (B needed a solo retry; runner's BSD-sed filter swallows tracebacks, worth fixing). THEN Tim supplied the first real Trio site: **user G = [trio-site] (token dynamicisf-…, in sites.json), Trio SHADOW mode since 2026-06-30 22:35Z, tz America/Denver (−6)**. Site double-posts each cycle (suggested+enacted seconds apart → 2 rows/cycle, dedupe by 5-min bucket); oref omits `units` when no SMB → treat NULL as 0. Pulled: 1,556 trio-shadow rows + 2,523 v1-silent (pre-tag stock Trio). First-glance (769 deduped paired cycles): Boost shadow ~8% MORE CONSERVATIVE than acting oref by day (39.1U vs 42.7U), ~20% at night; states IDLE 62%/OBS 24%/COMMITTED 7%/RECOV 5%/CONFIRMED 3%. Total DB now 355k rows, 8 users.

**PROMOTED 2026-07-06 (Tim's call): experimental == dev == `b2c0705e5e`, lockstep restored.** All 16 held commits live: tools rework (simulator/guide V1|V6 tabs + fixes, README corrections), early confirm + fast-path retune + gate telemetry, sensor fixes (step rollover, lump-tolerant wake), post-rescue cap (full version incl. docs), PR #15 + target-zone enhancement, cumulative-cap string fix, per-knob auto-config resolution (the user H rescue — fires on his next build install). Pre-promotion safety check: patch-id non-equivalents were the 3 adapted cherry-picks; content diff showed zero dev-only lines lost. Pages rebuilt with the reworked tools. APK `Boost-V6-experimental-promoted-2026-07-06.apk` → Drive (for Tim + cohort/user H thread).

HISTORICAL (superseded by the promotion): **ANALYSER PUSHED TO DEV ALONE (Tim's call, 2026-07-03 eve):** cherry-pick `e084e1fc64` → `cbcfb28c3d` pushed to origin/dev (Pages rebuilt — Trio-aware analyser LIVE). **dev/experimental divergence is now managed via cherry-picks** — origin/dev as of 2026-07-05 = 9d0a747584 + analyser cbcfb28c3d + post-rescue cap 79a3e53220 + **PR #15 merged (hartlijn, target-line 40% opacity — Tim's first external code PR; c7f9f0c7f6+7f68ed4ba9, ALSO cherry-picked back onto local experimental 2dd7b7f2ff+20cf9868b0)** + target-zone enhancement 92a4609d2a (= local 2ba3561ec8: TARGET_LINE → green #6ee7b7 50%/2px, TT-deviation zones green-raised/red-lowered, ProfileUtil DI added in BoostV2GraphData + OverviewModule). Everything on dev has an identical-content commit on local experimental → future promotion (push experimental → force-update dev, established 06-29 pattern) supersedes all cherry-picks cleanly. RULE: any PR merged into dev must ALSO be cherry-picked onto local experimental or promotion erases it.

**NIGHT — 24h telemetry review + 2 sensor fixes (commits 9-10, local):** Review verdicts: HR healthy (baseline warmup completes ~07-04, still default 70); new build healthy (flash 14:30, gate blocked 2 low-score promotions + passed 2 incl. 0.98U→0.95U delivered; knob=1.3 confirmed live); steps DEGRADED; sleep safe-but-60min-late wake; meal learner DEGENERATE (10 modes blanket the day incl. rebound pollution — corroborates onsets≈chance; recommendation: retire pre-meal target, Tim hasn't decided). Fixes shipped: `ecec9075b5` daily-rollover hold-higher — root cause was DailyStepHistoryTracker.phoneAnchoredWindow() cascade discarding UNCALIBRATED wear counts (<3 phone-overlap days, the `(raw)` flag) in favour of the phone's pocketed count, + HC mergeSource overwriting unconditionally; now max-of-candidates, upward-only completed-day revisions, `stepHistory: held wear X over phone Y` breadcrumb. `5f7a481f28` lump-tolerant wake — cumulative stepsToday growth (max wear/phone) over 60-min lookback ≥100 OR legacy bucket, AND ≥2 consecutive HR cycles above floor; 7-night NS replay: incident wakes 06:12 (−49min), 06-29 catches a genuine 04:33 nocturnal awakening (1000 real steps), ZERO false wakes; wake learner finally gets hr_steps samples. 160 tests green. mlMealG3Released "disappearance" = non-issue (event-conditional, V1/G3-only).

**PENDING: (1) push the remaining 9 commits (tools rework + 3 early-dosing + 2 sensor fixes) experimental→dev — held on Tim's preview approval + his live APK trial (SUPERSEDED same night: APK `Boost-V6-experimental-earlyconfirm+stepfix+wakefix-2026-07-03.apk` on Drive 22:50 with ALL 10 commits — Tim flashing tonight, fixes calculate from 2026-07-04 morning. Build gotcha reconfirmed: first assemble hit the stale-KSP ActivitiesModule false error → clean fixed it; ALSO: a failed build leaves the PREVIOUS APK at the output path — check the artifact timestamp before copying to Drive, a stale copy briefly went up mislabelled and was deleted); (2) 2026-07-10 live gate review; (3) extractor edits uncommittable (not a git repo); (4) Trio deep-comparison for user G as data accrues; (5) meal learner retire/keep decision.** Per [[feedback-boost-v6-branch-workflow]].

Same-day analyses: [[recovering-highs-smb-rejected-2026-07-03]], [[prediction-lines-validity-2026-07-03]].
