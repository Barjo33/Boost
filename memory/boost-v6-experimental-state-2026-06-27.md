---
name: boost-v6-experimental-state-2026-06-27
description: "Boost V6 state (2026-06-27 session): branches (experimental published vs wear-dynisf LOCAL-ONLY), V6 README/doc layout, hidden legacy plugins (V2/V3/v4.4/v4.4.2), Trio BoostMode, mlMealLikely lineage, and the full build+sign+deploy recipe (JDK, keystore, Drive, watch over adb)."
metadata: 
  node_type: memory
  type: project
  originSessionId: 069ebc90-c952-4f25-ad11-9de3daa100cb
---

**Boost-AAPS-core (`~/StudioProjects/Boost-AAPS-core`, remote `tim2000s/Boost-in-AAPS_3.4`)** — V6 line, state as of 2026-06-27.

**Repo/worktree layout (corrected 2026-07-02):** the V6 line lives in **`~/StudioProjects/Boost-AAPS-core`** (remote `origin` = tim2000s/Boost-in-AAPS_3.4) with **git WORKTREES** hanging off it (a worktree's `.git` is a FILE, not a dir — don't test with `[ -d .git ]`, it'll false-negative; use `git worktree list`). Current worktrees:
- `~/StudioProjects/Boost-AAPS-core` (main checkout) → **`Boost-V6-experimental`** ← the published branch, this is its real home.
- `~/StudioProjects/Boost-V6` → **`Boost-V6-wear-dynisf`** (has uncommitted article drafts). **MUST stay LOCAL — do NOT push to GitHub** (Tim deleted it from origin once). Mirrored experimental↔wear.
- `~/StudioProjects/Boost-V5-active` → `Boost-V5-active-alpha`; `~/StudioProjects/Boost-V6-activity-source` → `Boost-V6-activity-source`.
- `~/StudioProjects/AndroidAPS` is a **SEPARATE clone** (the `Boost-ML-Beta-shadow` mirror) that shares the SAME `origin` remote — so pushes from AndroidAPS move the same GitHub refs, but its remote-tracking refs are independent of Boost-AAPS-core's (each must fetch). Do V6-experimental work in Boost-AAPS-core, NOT AndroidAPS.

**BRANCH PROMOTION (2026-06-29):** Tim's fork pipeline is `experimental → dev (pre-master staging) → master (his primary)`. `Boost-V6-experimental` was **force-promoted to `dev`**. Old dev was backed up to **`dev-previous`** (`8935813a51`) first. Now on `tim2000s/Boost-in-AAPS_3.4`: **`dev` = the V6 line (`dc331e2a50`)**, `Boost-V6-experimental` kept as a background ref (== dev), `master` untouched (`3a1ca7e37f`). Rollback if needed: `git push origin dev-previous:dev --force`. Before the swap, the ONE genuine dev-only fix experimental lacked was cherry-picked onto both V6 branches — `0725f9e312` (UnitDoubleKey mmol/AAPSClient double-conversion in `JSONObjectExt.kt`); the other ~13 dev-only commits were no-op revert-pairs / cosmetic / retired-plugin / a `boost_simulator.html` dev tool, and the apparent "behind 3.4.2.2 / missing Tier-floor hypo fix" was FALSE (experimental already had both — verify by content/patch-id, NOT commit-count: `git log --cherry-pick --right-only`). Clean release build exit 0. The audit's forced safety relaxations (version-expiry bypass + allowAllBgSources) remain the **dev→master gate**, not done — see [[boost-v6-audit-2026-06-28]]. Gotcha: `isMaster()` returns TRUE on dev/experimental because appVersion `3.4.2.2` has no `-dev` suffix → the dirty-tree build guard still bites (stash drafts to build); a `-dev` marker would relax it (not yet done).

**CURRENT (verified 2026-06-28):** the two branches are **content-identical** — both diverge from merge base `57e21d57e6` (auto-config docs refresh, 06-26) and carry the **same 10 commits** (identical subjects, different hashes — applied to each independently); `git diff Boost-V6-experimental Boost-V6-wear-dynisf` is **empty**. So the "wear = superset" description below is historical; they are in sync. The 10 shared commits = the README-as-V6-front-page rewrite, hiding the 4 retired plugins (V2/V3/v4.4/v4.4.2-G3), the full **Boost-overview v2** label/contrast/pump-icon + profile-box work, and the **settings Advanced sub-screen** refactor. (Prior wear-only delta — V5→V6 rebrand, 2 review safety fixes, state-machine overview widget — is now present on both.)

**Docs (both branches):** `README.md` is now the V6 front page (what's different, how it works, settings, auto-config, HR/sleep night mode, and a backtesting / "no training" / robustness section). Legacy V1/V2/v4.2 settings moved to `docs/boost-v1-settings.md`. `BOOST.md` reduced to a pointer. Backtesting method lives in `backtesting/README.md`.

**Plugin visibility:** only **Boost** (V1 engine `openAPSBoost`, = shadow host + base) and **Boost V6** (`openAPSBoostV5`) are selectable. Hidden via `.showInList { false }` (code left in place, not deleted): **Boost V2** (`openAPSBoostV2`), **Boost V3** (`openAPSBoostV3`), **Boost v4.4** (`openAPSBoostV3ML`), **Boost v4.4.2 (G3)** (`openAPSBoostV3MLG3`). Architecture that made hiding safe: V6 (`OpenAPSBoostV5Plugin`) delegates ONLY to the V1 engine (`openAPSBoostEngine: Provider<OpenAPSBoostPlugin>`); V5 `runShadow` is invoked by V1 (and was also invoked by V3MLG3 as a 2nd shadow host). No keeper imports the hidden plugins.

**Trio** (`~/StudioProjects/Trio`, branch `Boost-in-Trio-v0.1`): V6-only via `BoostMode` enum (`off`/`shadow`/`active`) — no V2/v4.4/v4.4.2 selectable. "V4.4.2" there is only algorithm-lineage (`IsfShadowEma` ports the V4.4.2 EMA) + a replay test fixture.

**mlMealLikely** model (meal-likelihood, 50-tree, 8-feat, LOUO AUC 0.7375, asset `app/src/main/assets/boost/meal_likelihood_model.json`): evolved from v4.4 observability → V3MLG3 binary G3-hold gate (>0.65→0.50) → in V5/V6 a graded 0.20-weight term in the continuous `meal_signal_score` (MealSignalScore.kt); degrades gracefully (drops weight + renormalizes after ≥3 null cycles, tracked by `mlMealLikelyNullStreak`).

**Build & deploy locators (no secrets):**
- **JDK:** gradle needs `JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"` (no system Java; gradlew fails without it).
- **Signed phone APK:** from the `Boost-V6` worktree, `./gradlew clean :app:assembleFullRelease` (flavor full/release). **AndroidAPS release guard refuses a dirty tree** — `git stash -u` any uncommitted files (e.g. Tim's `diabettech-boost-v6-article*.md` drafts) first, build, then `git stash pop`. A stale incremental KSP cache causes a bogus `ActivitiesModule_ContributesHistoryBrowseActivity could not be resolved` error → fix with `clean`. Output: `app/build/outputs/apk/full/release/app-full-release.apk`. Signer CN=Tim [user].
- **Keystore:** `/Users/timstreet/Dev Tools/Keystore.jks`; creds in `keystore.properties` (gitignored, alias key0). NOT in memory by design. Worktrees need `keystore.properties` + `local.properties` copied in (gitignored, not shared across worktrees).
- **Drive (APK destination):** `/Users/timstreet/Library/CloudStorage/GoogleDrive-[user-Drive-account]/My Drive/Boost-v2-Analysis/` ([user-Drive-account] account — see [[drive-apk-destination]]).
- **Watch:** Oppo Watch X3 (Wear OS), deploy via `~/Library/Android/sdk/platform-tools/adb` over **Wireless debugging** (pair+connect). Watch-face source `~/StudioProjects/BoostWFFVariants` (flavors bgring/tir/hrring, appIds `com.boost.aaps.wff.{bgring,tir,hrring}`, task `:watchface:assemble<Flavor>Debug`). **bgring/tir BG-colour bug fixed 2026-06-27**: don't derive norm from RANGED_VALUE_MIN/MAX (Oppo returns VALUE normalised 0..1 but MIN/MAX raw → negative → stuck red); use magnitude `value>1.5 ? (v-40)/310 : value`. Fix committed + pushed `main` (`ee3b36d`, remote `github.com/tim2000s/BoostWFFVariants`), installed on-watch and verified green in-range. Watch was already paired — `adb mdns services` auto-discovers it (192.168.1.174:33569, model OWWE261); `adb install -r -t <apk>`.

**Overnight review 26→27 Jun (self/[self-NS-site]) + wear-HR fix:** Overnight mean 119(6.6) TIR 73% nadir 53(2.9) @00:33. Three findings, all explained: (1) the ~21:00 low = a **sensor-artefact** spike (BG jumped more than real) that triggered a 2U CONFIRMED commit (V1-would 1.9, so shared-detector not V6) → Tim has **engaged better smoothing**; low-handling itself was correct (0 SMB through the descent). (2) Morning high to 252(14.0) = **pump battery dying** around default-basal-attribution time (delivery gap, not under-dose). (3) **No wear HR all night/day.** Root cause (diagnosed live via adb on the Oppo OWWE261): HR-sampling toggle was OFF; after enabling, permissions were ALL already granted (`BODY_SENSORS` AND the newer Wear-OS health perm **`android.permission.health.READ_HEART_RATE`** both true — the Oppo gates the HR sensor behind READ_HEART_RATE) — but the listener still **did not register on the sensor**. Proof: `adb shell dumpsys sensorservice` → "Active sensors" showed Step_detector/Step_Sensor but **no HEART_RATE (0x15)**. **Only an AAPS-wear app force-stop+relaunch registered it** (then HEART_RATE connections=1; HR flowed: hrReadingsCount15m>0, bpm 79). So: enabling the pref toggle does NOT reliably (re)register HeartRateListener and logs nothing on failure → **code-fix TODO** (re-register on pref-enable + log/notify when BODY_SENSORS/READ_HEART_RATE missing or sensor yields no events). Also applied via adb: granted `BODY_SENSORS_BACKGROUND`, `dumpsys deviceidle whitelist +info.nightscout.androidaps`, appops RUN_(ANY_)IN_BACKGROUND allow, standby-bucket exempted; Tim disabled ColorOS overnight hibernation — together these target the separate "wear feed goes stale overnight (steps→phone)" Oppo-doze issue. Wear-HR diag recipe: `adb -t N shell dumpsys sensorservice | sed -n '/Active sensors/,/Socket Buffer/p'` (look for HEART_RATE 0x15) + `dumpsys package <pkg> | grep -iE "BODY_SENSORS|READ_HEART_RATE"`. Relates to [[boost_wear_hr_steps_2026-06-24]], [[feedback_sensor_reads_low_2026-06-16]].

**Design decision — V6 COB / tier-1&2 (2026-06-27, Tim):** V6 collapses the V4 8-tier ladder into `insulin = aggression_budget × action_multiplier(mealHypothesis)` (`MealActionMultiplier.kt`). COB is **still fully handled**: oref/`DetermineBasalBoost.kt` still computes mealCOB/CarbImpact/COBpredBG and folds the COB requirement into `insulinReq`, which V5 consumes as `baseInsulinReq`; COB also feeds the COB/UAM/IOB-blended `minGuardBG`. BUT the documented "Tiers 1,2: COB>0 → straight to COMMITTED" mapping is **intent only, NOT wired** — `DetermineBasalBoostV5.decide()`/`MealHypothesis` take no COB input; state is driven purely by `meal_signal_score`. So announced carbs before any BG rise sit in IDLE (1.0×, still passing through the oref COB dose) rather than an immediate aggressive commit. **Tim decided 2026-06-27 to LEAVE IT AS-IS** — do not wire the COB→COMMITTED transition; the tier-1/2 specialisation is intentionally not reproduced. Don't re-raise or "fix" this.

**V6 settings reorg (2026-06-27):** Simple mode is OUT (nobody uses it; it changes other GUI globally) — so advanced-hiding uses a **dedicated "Advanced" sub-screen**, NOT `defaultedBySM`. `OpenAPSBoostV5Plugin.addPreferenceScreen` now puts **8 essentials at top** (Aggression, HypoCaution, Max IOB, Max basal, NightMode enable + BG offset, SMB + UAM enable) and a **"Advanced"** PreferenceScreen (key `boost_advanced_settings`) holding the advanced V6 knobs + the grouped engine tree. Shared `addBoostEngineCategories` changed to take a `PreferenceGroup` + `includeEngineEssentials` flag; V6 passes `false` so the 8 essentials aren't duplicated in the sub-screens (dup keys break AndroidX); V1 "Boost" screen unchanged (default true). **Dev toggles removed + forced always-on in code:** `allow_all_bg_sources` (hard-coded true at both OpenAPSBoostPlugin read sites incl. the dosing one gating enableSMB_always/after_carbs) and `bypass_version_check` (VersionCheckerPlugin always returns maxIob). **V2 PROFILE pill** now shows profile name (getOriginalProfileName) + % only and taps to `uiInteraction.runProfileSwitchDialog` (meal state stays in the V5 strip). **Build guard gotcha:** can't compile-check uncommitted in the main repo (`isMaster() && !allCommitted()` throws) — commit first (locally), build from the Boost-V6 worktree. **COB/tier-1&2 decision:** left as-is (see above). All shipped to experimental (pushed) + wear-dynisf (local). APK `Boost-V6-PHONE-settings-reorg-2026-06-27f.apk` on Drive. **Trio settings reorg mirrored** (pushed `tim2000s/Trio` `Boost-in-Trio-v0.1` `306f743d0`): `FeatureSettingsView.swift` — top level = Mode/Aggression/HypoCaution/Night-mode(enable+offset), rest behind an "Advanced Boost Settings" NavigationLink (computed `advancedBoostSettings` view reusing the boostSlider helpers, no new file → no pbxproj). Max-IOB/basal/SMB/UAM are AAPS-engine-only (Trio global settings, not in the Boost section). Trio Xcode build (iPhone 17 sim) SUCCEEDED. UI-only; BoostV5Core untouched.

**This session's deliverables (2026-06-27):** (1) rewrote the V6 README + moved legacy settings out; (2) hid the 4 retired plugins; (3) raised V2-overview label contrast `#444444`→`#9aa0ad` (WCAG AA) on both branches; (4) fixed + shipped the BG-ring watch-face colour bug to GitHub and the watch; (5) built a **signed phone APK from `Boost-V6-wear-dynisf`** and put it on Drive as `Boost-V6-PHONE-V2contrast+hiddenplugins-2026-06-27.apk`. Latest `Boost-V6-experimental` HEAD after the 06-27 session = the V2-contrast commit (`b6ed6c6e30`). **Superseded — as of 2026-06-28 both branch HEADs are the settings Advanced-sub-screen refactor** (experimental `a5640ec517`, wear-dynisf `2b18e23302`; same tree).

Related: [[boost-v5-autoconfig]], [[boost-running-build-and-v5-overnight]], [[boost-wff-watchface]], [[drive-apk-destination]].
